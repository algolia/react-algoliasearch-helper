#Experimental provider/connector for Algolia's JS

## Usage

 - Wrap the root of your app with the `Provider`
 - Use the `connect` HOC on your search enabled component. It provides an `helper` instance and the `results` of the latest search.
 - ...
 - Profit!
